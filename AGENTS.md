# Learning about effect

First read `vendor/effect/LLMS.md`. Use `vendor/effect` for learning about
effect, forget all previous knowledge about effect and avoid using node_modules.

# When working on frontend

You must use the `frontend-design` and `shadcn` skills.

Run `pnpm dev` in the background to start the dev services.

# Reading ManagerClient.ts

Be careful reading `packages/manager-api/src/ManagerClient.ts`, as it is a large file. Never read the entire file at once.
